#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * Description: Program that prints its name
 * @argc: No. of command line arguements
 * @argv: Poniter to array of characters
 *
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
